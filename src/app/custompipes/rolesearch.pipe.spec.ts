import { RolesearchPipe } from './rolesearch.pipe';

describe('RolesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new RolesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
